# P1-3.2 백필 SQL/절차 실행 런북 (멱등/재실행 가능)

- Task ID: `10000000-0000-4000-8000-000000000050`
- 작성 시각: `2026-03-02 13:33:45 KST`
- 대상 Supabase 프로젝트: `every-shift-mvp` (`vjmerqaxguovnojinxfq`)
- 대상 마이그레이션 파일: `migrations/009_backfill_service_fields.sql`

## 1) 목적 및 범위

본 런북은 P1-3.2 구현 산출물의 실행 절차와 검증 기준을 정의한다.

- 멱등 백필 대상:
1. `organization_settings`
2. `sites` (`MAIN`/`본원`)
3. `ranks` (`RN`/`일반간호사`)
4. `skills` (`GENERAL`/`기본간호`)
5. `profiles` / `organization_memberships` (관리자 1계정 한정)
6. `site_staffing_requirements` (legacy `site_requirements` 이관)
7. `schedule_assignments.site_id` (운영 조직 + `NULL`만 채움)
- 정책 유지 대상:
1. `employees.user_id` (본 단계 미변경)

## 2) 구현 요약 (B001~B009)

`migrations/009_backfill_service_fields.sql`는 단일 트랜잭션(`BEGIN/COMMIT`)으로 구성된다.

1. `B001`: 운영 조직 추출 + `admin@everyshift.com` 단일성 강제(`RAISE EXCEPTION`)
2. `B002`: `organization_settings` 삽입 (`ON CONFLICT (organization_id) DO NOTHING`)
3. `B003`: `sites` 기본값 삽입 + 운영 조직별 `MAIN` 사이트 존재 검증
4. `B004`: `ranks` 기본값 삽입 (`ON CONFLICT`)
5. `B005`: `skills` 기본값 삽입 (`ON CONFLICT`)
6. `B006`: 관리자 전용 `profiles` / `organization_memberships` upsert
7. `B007`: `site_staffing_requirements` 이관 (`NOT EXISTS` 가드)
8. `B008`: `schedule_assignments.site_id` 업데이트 (`WHERE sa.site_id IS NULL`)
9. `B009`: `employees.user_id` 미변경 정책 주석화

## 3) Preflight 재측정 결과 (PRE-01~PRE-08)

측정 시각: `2026-03-02 13:33:45 KST`

### PRE-01 핵심 row count

| table_name | row_count |
| --- | ---: |
| organizations | 5 |
| employees | 19 |
| schedules | 2 |
| schedule_assignments | 317 |
| site_requirements | 21 |
| organization_settings | 0 |
| sites | 0 |
| ranks | 0 |
| skills | 0 |
| site_staffing_requirements | 0 |
| profiles | 0 |
| organization_memberships | 0 |

### PRE-02 운영 조직 판정

- 운영 조직 1건:
1. `00000000-0000-0000-0000-000000000001` (`세브란스병원`)

### PRE-03 NULL 분포

- `employees.user_id`: `19 NULL / 0 filled`
- `schedule_assignments.site_id`: `317 NULL / 0 filled`
- `site_requirements.site_id`: `21 NULL`
- `site_requirements.skill_id`: `21 NULL`
- `site_requirements.rank_id`: `21 NULL`

### PRE-04/05 무결성

- PRE-04 (`site_requirements.organization_id` vs `shifts.organization_id`): `mismatch_count = 0`
- PRE-05 (`schedule` vs `employee` 조직 일치): `mismatch_count = 0`

### PRE-06 중복 위험

- `site_requirements(org/shift/day)` 중복 결과: `0행` (중복 없음)

### PRE-07 인덱스 드리프트

- 확인됨:
1. `uq_site_requirements_scope`
- 미확인:
1. `uq_employees_org_user`
2. `uq_site_staffing_requirements_scope`

### PRE-08 migration ledger

- `supabase_migrations.schema_migrations`:
1. `20260213034303 | 004_add_off_comment`

## 4) 실행 절차

### 4.1 권장 실행 순서

1. PRE-01~PRE-08 재실행
2. `009_backfill_service_fields.sql` 적용
3. POST-01~POST-07 실행
4. 결과를 본 문서 7장 형식으로 기록

### 4.2 실패 시 롤백/재시도

1. 본 SQL은 단일 트랜잭션이므로 실패 시 전체 롤백된다.
2. 실패 원인(계정 단일성, FK, 권한, 드리프트)을 수정한다.
3. 동일 SQL을 재실행한다.
4. 재실행 시 `ON CONFLICT`/`NOT EXISTS`/`IS NULL` 가드로 중복/오염을 방지한다.

## 5) 사후 검증 (POST-01~POST-07)

검증 SQL은 기존 카탈로그를 재사용한다.

- 파일: `docs/migration/sql/p1_3_1_validation_queries.sql`
- 실행 블록:
1. `POST-01`: 마스터 백필 완료성
2. `POST-02`: `site_staffing_requirements` 건수 일치
3. `POST-03`: `schedule_assignments.site_id` NULL 해소
4. `POST-04`: FK orphan = 0
5. `POST-05`: target scope 중복 = 0
6. `POST-06`: `site_requirements` 원본 건수 보존
7. `POST-07`: `employees.user_id` 정책 유지

## 6) 테스트 시나리오 정의

1. 1회 실행: POST-01~07 전체 통과
2. 2회 실행: 추가 변경건 0 (멱등성)
3. 관리자 이메일 미존재/중복: B001 예외로 전체 중단
4. 부분 선반영 상태: 누락분만 보충
5. 드리프트 지속 상태: `NOT EXISTS` 가드로 중복 방지

## 7) 실행 증적 (이번 세션)

### 7.1 실제 실행 상태

- 마이그레이션 적용 시도:
1. `apply_migration(name='009_backfill_service_fields')`
- 결과:
1. 실패: `Cannot apply migration in read-only mode.`

### 7.2 환경 확인 증적

- `select current_setting('transaction_read_only');`
- 결과: `on`
- `show default_transaction_read_only;`
- 결과: `on`
- `select current_user, session_user;`
- 결과: `supabase_read_only_user / supabase_read_only_user`

### 7.3 POST-01~POST-07 기준선(적용 전)

- POST-01:
1. `organization_settings_cnt=0`
2. `sites_cnt=0`
3. `ranks_cnt=0`
4. `skills_cnt=0`
- POST-02:
1. `source_count=21`
2. `target_count=0`
- POST-03:
1. `remaining_null_site_id=317`
2. `total_assignments=317`
- POST-04:
1. `orphan_assignment_site=0`
2. `orphan_requirement_site=0`
3. `orphan_requirement_skill=0`
4. `orphan_requirement_rank=0`
- POST-05: `0행` (중복 없음)
- POST-06:
1. `expected_site_requirements_count=21`
2. `current_site_requirements_count=21`
3. `delta=0`
- POST-07:
1. `user_id_null_count=19`
2. `user_id_filled_count=0`
3. `total_employees=19`

### 7.4 쓰기 세션/프로젝트 전환 시도 결과

1. `every-shift-mvp (vjmerqaxguovnojinxfq)`:
- `default_transaction_read_only=on`
- 계정: `supabase_read_only_user`
- `apply_migration` 실패: `Cannot apply migration in read-only mode.`
- `execute_sql` 직접 실행 실패: `permission denied for table organization_settings`

2. `every-shift-admin (mzlmujbyixrjyxzozfuw)`:
- `default_transaction_read_only=on`
- 계정: `supabase_read_only_user`
- 동일 쓰기 권한 한계 확인

3. `every-shift-vben (kkxchntkzopfrpnvzzth)`:
- `INACTIVE`
- SQL 실행 시 connection timeout

### 7.5 POST-01~POST-07 재실행 및 기준선 비교 (2026-03-02 13:40:42 KST)

- 비교 결과: **기준선 대비 전 항목 동일 (delta 없음)**  
  (실제 DML이 권한 오류로 커밋되지 않았기 때문)

| Check | 기준선 | 재실행 | 비교 |
| --- | --- | --- | --- |
| POST-01 `organization_settings/sites/ranks/skills` | `0/0/0/0` | `0/0/0/0` | 동일 |
| POST-02 `source/target` | `21/0` | `21/0` | 동일 |
| POST-03 `remaining_null_site_id` | `317` | `317` | 동일 |
| POST-04 orphan 합계 | `0` | `0` | 동일 |
| POST-05 duplicate rows | `0` | `0` | 동일 |
| POST-06 `current/delta` | `21/0` | `21/0` | 동일 |
| POST-07 `user_id_null/filled` | `19/0` | `19/0` | 동일 |

### 7.6 판정

- 본 세션에서는 Supabase가 read-only 상태여서 DB 변경(백필 DML)까지는 수행하지 못했다.
- 코드 산출물(`009_backfill_service_fields.sql`)과 실행 절차/검증 기준은 확정되어, 쓰기 권한 복구 후 즉시 적용 가능하다.

### 7.7 사용자 콘솔 실행 완료 후 결과 확인 (2026-03-02 13:44:22 KST)

- 사용자 보고: Supabase SQL Editor에서 `009_backfill_service_fields.sql` 실행 완료
- MCP 재측정 결과:
1. POST-01: `organization_settings/sites/ranks/skills = 1/1/1/1`
2. POST-02: `source/target = 21/21`
3. POST-03: `remaining_null_site_id = 0`
4. POST-04: orphan 합계 `0`
5. POST-05: duplicate rows `0`
6. POST-06: `current/delta = 21/0`
7. POST-07: `user_id_null/filled = 19/0` (정책 유지)

| Check | 기준선 | 실행 후 | 비교 |
| --- | --- | --- | --- |
| POST-01 `organization_settings/sites/ranks/skills` | `0/0/0/0` | `1/1/1/1` | 개선 |
| POST-02 `source/target` | `21/0` | `21/21` | 개선 |
| POST-03 `remaining_null_site_id` | `317` | `0` | 개선 |
| POST-04 orphan 합계 | `0` | `0` | 유지 |
| POST-05 duplicate rows | `0` | `0` | 유지 |
| POST-06 `current/delta` | `21/0` | `21/0` | 유지 |
| POST-07 `user_id_null/filled` | `19/0` | `19/0` | 정책대로 유지 |

### 7.8 최종 판정

- P1-3.2 요구사항(멱등 백필 SQL + 절차 문서화 + 사후 검증)을 충족했다.
- 백필 핵심 대상은 모두 반영되었고, 무결성/중복/원본보존/정책유지 조건도 통과했다.

## 8) Task 50 검증 기준 매핑

| Task 50 기준 | 본 산출물 반영 내용 |
| --- | --- |
| 단계별 절차 정의 | B001~B009 단계 정의 + 실행 순서/실패 처리 명시 |
| 멱등/재실행 안전성 | `ON CONFLICT`, `NOT EXISTS`, `WHERE IS NULL`, 단일 트랜잭션 적용 |
| 중복/오염 위험 최소화 | 운영 조직 한정, admin 단일성 강제, POST-01~07 검증 연결 |
