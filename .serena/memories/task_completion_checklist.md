# Task Completion Checklist
- Run relevant validation: `pnpm lint` and targeted tests (`pnpm test:unit` or `pnpm test:e2e`).
- Confirm MVP constraints unchanged (no out-of-scope features).
- Confirm Korean UI text for user-facing strings.
- Confirm createDiscreteApi usage conventions for Naive UI global feedback.
- Summarize changed files and residual risks.